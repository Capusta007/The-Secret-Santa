server:
  port: 8080

spring:
  application:
    name: api-gateway

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
    config-url: /v3/api-docs/swagger-config
    urls:
      - name: Gateway Service API
        url: /v3/api-docs